# Secret: tidak pernah di-commit. Kunci burner dibangkitkan skrip, bukan diketik tangan.
.env
.env.*
!.env.example
**/keystore/

# Build & cache
out/
cache/
broadcast/
node_modules/

# Junction ke app/ — jangan ikut terlacak sebagai isi repo ini
/lib/

.DS_Store
Thumbs.db

# Riwayat aksi agen: berisi secret commit dan alamat lokal. Bukti yang benar ada di
# chain (event Action + transcriptHash), jadi file ini tidak perlu ikut repo.
agent/history/

# State runtime lokal: pid satu-satunya loop agen yang hidup.
agent/*.lock
